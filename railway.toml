[build]
builder = "nixpacks"

[deploy]
startCommand = "/opt/venv/bin/python3 python-ai/app.py"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
